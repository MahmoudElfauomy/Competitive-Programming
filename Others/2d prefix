// 2d-prefix (1-based)
for (int i = 1; i <= n; i++)
{
    for (int j = 1; j <= m; j++)
    {
        pre[i][j] = pre[i - 1][j] + pre[i][j - 1] - pre[i - 1][j - 1] + v[i][j];
        cout << pre[i][j] << ' ';
    }
    cout << endl;
}
